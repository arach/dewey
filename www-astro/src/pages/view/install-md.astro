---
import AgentFileLayout from '../../layouts/AgentFileLayout.astro'

const raw = await import('../../../install.md?raw')
const content = raw.default
const mod = await import('../../../install.md')
const { Content } = mod
---
<AgentFileLayout
  title="install.md"
  fileName="install.md"
  content={content}
  rawPath="/install.md"
  renderable={true}
>
  <Content />
</AgentFileLayout>
